extends layout

block content
    h1 Subject Details

    if subject
        table(border=1)
            tr
                th ID
                td #{subject.id}
            tr
                th Subject Name
                td #{subject.name}

        p
            a(href="/subjects") Back to Subjects
    else
        p Subject not found.
        p
            a(href="/subjects") Back to Subjects